# Agent Note: Bind migration qualification to executed packaged evidence

Status: implemented

English | [中文](2026-09-10-packaged-migration-qualification.zh.md)

## Decision

The [Accepted integration decision](../../../../docs/decisions/20260910-desktop-mint-target-integration.md) and its fixed scenario registry govern migration qualification. A truthful unverified source assessment permits adoption; publication additionally requires complete, independently architecture-bound packaged reports. Source test results cannot stand in for normal native behavior.

## Implementation

The qualifier authenticates the delivered baseline, exact candidate checkout and DMGs, actual packaged runtimes, complete built-in composition and retained evidence. Each scenario selects its own executed assertions. Native fixtures preserve the product sandbox and use disposable runner accounts; missing capability refuses qualification. Promotion and retained restoration revalidate report payloads; withdrawal can still hide an exact damaged release.

## Verification

Focused parser, ancestry, recovery, process cleanup and debugger protocol regressions accompany real isolated packaged backend migration, reopening and complete backup restoration. The keyless persona/seeded-query scenario records scoped model-visible input and cold query output. Normal native UI, shell support and both architecture production reports remain execution gates in protected CI; local development receipts explicitly remain unqualified.

## Alternatives

Relabeling frozen source assessments or sharing opaque per-architecture payloads would erase provenance. Rewriting authored data or launching the baseline on upgraded roots would invalidate recovery. Retained byte inventories bind the actual backup and upgraded copies before successful synthetic fixture cleanup; failures retain diagnostics.
