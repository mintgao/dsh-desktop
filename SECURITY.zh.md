# 安全政策

[English](SECURITY.md) | 中文

请通过 `mintgao/dsh-desktop` 的 GitHub 私密漏洞报告功能提交安全问题。疑似凭据泄露、任意代码执行、沙箱逃逸、签名绕过或会话数据暴露时，请勿创建公开 Issue。

报告应包含受影响的 DSH Desktop 版本与 Mac 架构、最小复现步骤、影响和可行的缓解方案。所有附件都必须移除 API key、凭据、个人会话内容与私有 workspace 数据。

维护者会通过私密安全公告确认完整报告，并在修复或缓解方案可用后协调披露。若漏洞不依赖桌面壳、可在 DeepSeek Harness 中直接复现，还应通过上游项目公布的安全渠道提交。

只有附加在 `desktop-v*` GitHub Release 上、经过签名和公证的产物属于受支持的分发版本。本地构建或修改过的应用不在发布签名保证范围内。
