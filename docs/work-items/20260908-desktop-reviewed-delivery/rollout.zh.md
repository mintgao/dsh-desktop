# 经审查交付上线前提

[English](rollout.md) | 中文

## 状态

远程准备已部分执行，启用仍受阻。[验证记录](verification.zh.md) 包含实际观察到的保护与基线证据；[已接受决策](../../decisions/20260908-desktop-reviewed-delivery.zh.md) 规定引导与启用。本地验证不能证明实际 GitHub 批准、通知送达或发布。

## 已授权准备结果

所有者于 2026-09-08 授权上线准备。已复核仓库和 PR 64 身份，无变化。已创建引导不可变规则集 `22549859` 和不含秘密的 `mint-delivery-bootstrap` 环境；该环境要求所有者批准，仅允许 `desktop-bootstrap-*` 标签。所有者另行明确授权合并的 PR 创建与审查能力后，仓库设置为 `can_approve_pull_request_reviews: true`，默认令牌权限仍只读。最初的自动审查拒绝已通过这项具体授权解决。

待推送候选使用基于 `main` 提交 `089d92d9f6e051472c2522af32aac75ed3e04742` 的独立工作树，保留实际开发目录，排除无关恢复改动。在所需引导证据成功前，旧工作流、App 权限及 PR 64 保持不变。公开版本和已安装应用均未改变。

## 待审查的确切仓库变更

目标为 `mintgao/dsh-desktop`，仓库 ID 为 `1344813014`。保留现有公开版本和不可变标签。

| 控制 | 已观察身份 | 所需结果 |
|---|---|---|
| 主分支保护 | 规则集 `21944549` | 保留审查及最后推送批准，要求 `Desktop source checks`，移除 App 绕过 |
| 桌面标签创建 | 规则集 `21944575` | 保留维护者创建，移除 App 绕过 |
| 桌面标签不可变性 | 规则集 `21944588` | 保留无绕过的更新与删除保护 |
| 发布批准 | 环境 `mint-publication` | 受保护分支限制，加上必需所有者 ID `154321591`；允许唯一所有者显式批准 |
| Actions 默认值 | 工作流权限设置 | 默认只读 token，启用机器人 PR 创建 |
| 首次引导 | 专用标签前缀与不含秘密的批准环境 | 绑定已审查实现快照，验证实际机器人作者及最后推送者 |
| 旧写入者 | 发行配置中的工作流清单 | 启用前禁用入口、排空任务并验证尚存权限已撤销 |
| 替代机制启用 | 经过审查且受源码管理的启用记录 | 绑定已验证控制、历史基线和迁移报告 |

三个旧 App ID 为 `4782984`、`4783001` 和 `4783016`。只移除工作流引用或存储的秘密，不能证明尚存权限已撤销。保留管理员证据和历史证明，不把它们当作当前发布权限证明。

## 保留待处理采用

获得所有者明确授权后，关闭[旧 PR 64](https://github.com/mintgao/dsh-desktop/pull/64)，不合并，也不删除分支。关闭前验证仓库、PR 编号、分支 `automation/adopt/dsh-v0.1.2-alpha.4` 和头提交 `b02b0bc1ed4492715b7003a0b19b8fe38901cd20`；身份变化时阻止协调。最终验证关闭前，禁用并排空旧写入者。

以持久 Git 引用保留该确切提交、PR 历史及固定的旧状态。提交包含 `scripts/upstream-adoption/align-downstream-package-versions.mjs` 和独立的下游工作区版本修复，后续 alpha.4 采用必须检查并复用或明确协调这些修复。将处置记录为“旧 PR 已关闭；源码已保留供替代采用使用”。发布基线和源码锁仍保持 alpha.3。

重新运行完整发现和迁移预检。Alpha.4 必须仍是下一个有序上游版本，且不存在未解决的旧采用 PR。最终启用报告绑定实际观察到的处置。保留、关闭或验证失败均阻止启用。记录本计划没有执行关闭。

## 待推送基线验证

独立 `main` 基线上的两次完整检查均仅在未改动的 spill 清理边界夹具失败；lint、typecheck 和 build 通过。夹具将请求的时间戳与文件系统舍入后的时间戳比较。独立的支持性测试修正使用实际读取的文件时间作为确切截止点，保留断言及产品清理行为。其他恢复改动仍排除。引导可见性使用经过审查的 seed 所有管理员证据及新鲜规则验证。

## 授权后的验收

检查实际机器人作者身份和所有者审查资格时，保持首个机器人 PR 打开。独立且经所有者批准的引导探针使用确切的非公开草稿，验证普通 Actions token 的资产上传、读取、删除和可逆正文编辑。必须成功清理；探针不能发布草稿或改变目标。

协调待处理采用并撤销旧写入者后，采集最终管理员预检与基线证据。将匹配的证据和启用配置更新到同一个首个 PR，再审查最终头提交。首次合并同时引入普通工作流和已验证启用；若先合并未启用的中间版本，引导机制会在普通采用能够运行前退出。

双原生架构及新批准清单先于任何特定公开预览。ARM64 诊断检查不能替代该发布资格验证。本上线安排本身不授权公开发布或替换已安装应用。

## 首次实际引导

已推送经审查 seed `4944752aa6927b23ae73b18c631a4340b2584ef8` 及受保护标签 `desktop-bootstrap-reviewed-20260908-4944752aa6`。所有者批准的运行 `34238478819` 进入引导执行，但在创建 PR 前拒绝规则集时间戳投影。管理员 `2026-09-08T21:49:50.402+08:00` 和公开 `2026-09-08T13:49:50.402Z` 表示同一时刻，其他比较字段均一致。经过审查的时间戳修正需要新 seed 和计划。未发生旧机制切换或发布。

## 时间戳候选验证

共享时间戳修正通过针对性测试、独立审查和全部 32 项文档检查；保留的管理员响应字节未变。完整验证的 lint、typecheck 和 build 通过，17,296 项测试通过、116 项跳过，但未改动的 HMR 缺失父目录创建测试失败。同一用例在主机单独运行通过，完整运行中的原生监听超时仍未解决。该候选尚未提交或作为新引导 seed 调度。所有者已完成重新验证身份；实际 Controller 安装页面确认其访问仅限仓库 `1344813014`。引导验证成功前，旧权限保持不变。

## 支持性就绪修正

[配置就绪工作项](../20260908-config-watch-readiness/brief.zh.md) 记录已批准并通过独立验证的目标采样器。全部 HMR 验收用例通过。合并检查仍被主分支的压缩耗时断言以及 UI 清理后的表格滚动回调阻止；局部 fixture 修正保留产品行为，必须通过独立验证后才能调度新的引导 seed。

## 修正后候选验收

两项局部 fixture 修正后，独立 QA 通过配置中的完整 lint、typecheck、test 和 build 检查：17,306 项测试通过、116 项跳过，没有未处理异常。运行期间全部 37 个候选文件哈希及已提交基线保持不变。HMR 采样器与时间戳修正通过本地验收；实际引导、机器人审查资格、草稿访问和旧机制切换仍待验证。
