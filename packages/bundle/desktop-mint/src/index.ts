/**
 * @deepseek-ai/dsh-desktop-mint — the DSH Desktop Mint product Bundle.
 * Its patch selects product plugins and defaults over the shared Web surface;
 * this module carries no runtime API.
 * @module @deepseek-ai/dsh-desktop-mint
 */

export {}
